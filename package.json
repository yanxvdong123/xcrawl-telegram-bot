{
  "name": "xcrawl-scraper-telegram-bot",
  "version": "1.0.0",
  "description": "Telegram bot that scrapes web pages using XCrawl API — send a URL, get structured data back",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.66.0",
    "axios": "^1.7.0"
  }
}
